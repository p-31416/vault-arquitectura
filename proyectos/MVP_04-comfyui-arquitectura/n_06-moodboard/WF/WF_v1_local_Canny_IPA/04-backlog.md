---
tipo: proyecto
fase: concepto
fecha_creacion: 2026-09-09
ultima_actualizacion: 2026-09-09
tags: [mvp_04, n_06, backlog]
---

# 04-backlog

- [ ] Instalar `sqlalchemy` en venv Comfy y arrancar DirectML, luego `node_info` (CL/CN/IPA/CLIPVision) + `validate` + `enqueue` batch L1+L2.
- [ ] Confirmar `architecturerealmix_v11` + `control_v11p_sd15_canny` visibles (hoy MCP solo lista Juggernaut + scribble).
- [ ] Instalar `ComfyUI_IPAdapter_plus` cuando L1 valide, luego activar L2 IPA 0.8 + style 0.65.
- [ ] Generar `salidas/n_06-B-01.png` (+ `-B-02`) y promover final a `activos/proyectos/<obra>/renders/`.
- [ ] Fichas `tipo: referente` con `curaduria` para cada input (sin ficha no entra).
